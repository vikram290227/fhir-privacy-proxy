apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhir-privacy-proxy.fullname" . }}
  labels:
    {{- include "fhir-privacy-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fhir-privacy-proxy.selectorLabels" . | nindent 4 }}
